# project_module_2

- Проект создавался на conda python 3.13.9.

- Библиотеки в requirements.txt добавлены вручную.

- Финальное выполнение тестировалось на новом окружении python 3.14.3 с установкой библиотек из файла.

- Установка библиотек pip install -r requirements.txt из Bash.

- Создать файл env. 
DB_USER=пользователь
DB_PWD= пароль в двойных скобках. ""
DB_HOST=хост
DB_PORT=порт
DB_NAME=база данных